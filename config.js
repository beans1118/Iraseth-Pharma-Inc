/* =========================================================
   IRASETH PHARMA — Frontend config
   Point this at your running backend (see /backend/README.md).
   If the API can't be reached, the site automatically falls back
   to its built-in offline demo data — nothing breaks either way.
   ========================================================= */
const API_BASE = "http://localhost:5000/api";
